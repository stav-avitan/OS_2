#ifndef _CODECB_H_
#define _CODECB_H_

#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <dlfcn.h>
#include <string.h>

void codecB_encode(char* str);
void codecB_decode(char* str);

#endif